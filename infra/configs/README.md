# infra/configs

Static configuration files for infrastructure services (reverse proxy rules, CDN config, etc.).

**Status: Not yet populated.**

Environment-specific secrets and connection strings belong in Vercel environment variables or a secrets manager — never in this directory.

Files committed here must be safe to check in (no credentials, no tokens).
